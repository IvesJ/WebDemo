# 车机H5混合视频播放器

一个专为Android车机应用设计的混合视频播放器解决方案，支持在H5页面内流畅播放视频，同时满足车机音频焦点策略和杜比音效需求。

## 核心特性

### 🎯 双渲染模式
- **Canvas渲染模式**：适用于列表流场景，视频在H5 Canvas中渲染，滚动零延迟
- **同层渲染模式**：适用于详情页/全屏播放，使用SurfaceView硬件加速，画质更优

### 🎵 音频能力
- **音频焦点管理**：完整支持Android AudioFocus机制，适配车机音频策略
- **杜比音效支持**：支持Dolby Digital、Dolby Digital Plus、Dolby Atmos等格式
- **焦点类型可配置**：支持GAIN、TRANSIENT、TRANSIENT_MAY_DUCK、EXCLUSIVE等焦点类型

### 🚀 性能优化
- 自动选择最优渲染方案（根据视频尺寸和场景）
- 动态帧率调整（根据滚动速度）
- 使用Choreographer实现60fps同步刷新
- 硬件加速支持

### 📱 特性
- 完全支持H5内播放，保持原生H5体验
- 支持页面滚动时视频位置同步
- 支持多实例播放器管理
- 完整的播放控制API（播放/暂停/跳转/音量等）
- 丰富的事件回调

## 架构设计

```
┌─────────────────────────────────────────┐
│           H5 Layer                      │
│  ┌──────────────────────────────────┐   │
│  │  HybridVideoPlayer.js (SDK)      │   │
│  └──────────────────────────────────┘   │
│              ↕ JSBridge                 │
└─────────────────────────────────────────┘
                  ↕
┌─────────────────────────────────────────┐
│         Native Layer                    │
│  ┌──────────────────────────────────┐   │
│  │  HybridVideoPlayerManager        │   │
│  └──────────────────────────────────┘   │
│       ↓                    ↓             │
│  ┌─────────────┐    ┌──────────────┐    │
│  │  Canvas     │    │  Layer       │    │
│  │  Renderer   │    │  Renderer    │    │
│  └─────────────┘    └──────────────┘    │
│  ┌──────────────────────────────────┐   │
│  │  AudioFocusManager               │   │
│  │  DolbyAudioProcessor             │   │
│  └──────────────────────────────────┘   │
│  ┌──────────────────────────────────┐   │
│  │  ExoPlayer                       │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

## 快速开始

### 1. 项目依赖

已在`build.gradle.kts`中配置：

```kotlin
// ExoPlayer - 视频播放核心
implementation("androidx.media3:media3-exoplayer:1.2.0")
implementation("androidx.media3:media3-exoplayer-dash:1.2.0")
implementation("androidx.media3:media3-exoplayer-hls:1.2.0")
implementation("androidx.media3:media3-ui:1.2.0")
```

### 2. 原生侧集成

在Activity中初始化：

```kotlin
class MainActivity : AppCompatActivity() {
    private lateinit var webView: WebView
    private lateinit var playerManager: HybridVideoPlayerManager

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        // 初始化播放器管理器
        playerManager = HybridVideoPlayerManager.getInstance(this)
        playerManager.setWebView(webView)

        // 设置JSBridge
        val jsBridge = VideoPlayerJSBridge(webView, playerManager)
        webView.addJavascriptInterface(jsBridge, "NativeVideoPlayer")

        // 加载H5页面
        webView.loadUrl("your_url")
    }
}
```

### 3. H5侧使用

引入SDK：

```html
<script src="js/HybridVideoPlayer.js"></script>
```

创建播放器实例：

```javascript
// 创建播放器
const player = new HybridVideoPlayer({
    containerId: 'video-container',  // 容器元素ID
    renderMode: 'auto',              // 渲染模式: 'canvas' | 'layer' | 'auto'
    audioFocusType: 'transient',     // 音频焦点类型
    enableDolby: true,               // 是否启用杜比音效
    maxFrameRate: 60,                // 最大帧率
    autoPlay: false                  // 是否自动播放
});

// 播放视频
player.play('https://example.com/video.mp4');

// 监听事件
player.on('playing', () => {
    console.log('开始播放');
});

player.on('progress', (currentTime, duration) => {
    console.log(`进度: ${currentTime}/${duration}`);
});

// 控制播放
player.pause();
player.seek(30);  // 跳转到30秒
player.setVolume(0.8);  // 设置音量
player.setMuted(true);  // 静音

// 销毁播放器
player.destroy();
```

## API文档

### HybridVideoPlayer 构造参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| containerId | string | - | 容器元素ID（必填） |
| renderMode | string | 'auto' | 渲染模式：'canvas'、'layer'、'auto' |
| audioFocusType | string | 'transient' | 音频焦点类型：'gain'、'transient'、'transient_may_duck'、'exclusive' |
| enableDolby | boolean | false | 是否启用杜比音效 |
| maxFrameRate | number | 60 | Canvas模式最大帧率 |
| autoPlay | boolean | false | 是否自动播放 |
| loop | boolean | false | 是否循环播放 |
| muted | boolean | false | 是否静音 |
| volume | number | 1.0 | 初始音量（0.0-1.0） |

### 方法

| 方法 | 参数 | 说明 |
|------|------|------|
| play(url) | url: string | 播放指定URL的视频 |
| pause() | - | 暂停播放 |
| seek(position) | position: number | 跳转到指定位置（秒） |
| setVolume(volume) | volume: number | 设置音量（0.0-1.0） |
| setMuted(muted) | muted: boolean | 设置静音状态 |
| setPlaybackSpeed(speed) | speed: number | 设置播放速度（0.5-2.0） |
| destroy() | - | 销毁播放器 |
| getState() | - | 获取当前状态 |
| getCurrentTime() | - | 获取当前播放位置（秒） |
| getDuration() | - | 获取视频总时长（秒） |
| getVideoSize() | - | 获取视频尺寸 {width, height} |

### 事件

| 事件名 | 参数 | 说明 |
|--------|------|------|
| prepared | - | 准备完成 |
| ready | - | 准备完成（同prepared） |
| playing | - | 开始播放 |
| paused | - | 暂停播放 |
| buffering | - | 缓冲中 |
| ended | - | 播放完成 |
| error | error: {code, message} | 发生错误 |
| progress | (currentTime, duration) | 播放进度更新 |
| timeupdate | currentTime | 时间更新 |
| statechange | state | 状态改变 |
| videosizechange | (width, height) | 视频尺寸改变 |
| bufferingupdate | percent | 缓冲进度更新 |

## 渲染模式选择

### Canvas模式（推荐用于列表流）

**优点：**
- 视频在H5 Canvas中渲染，滚动零延迟
- 完美支持快速滑动
- 多个视频同时播放性能好

**缺点：**
- 帧数据传输有开销
- 画质略低于原生

**适用场景：**
- 短视频列表流（类似抖音）
- 多视频同屏显示
- 需要频繁滚动的场景

### Layer模式（推荐用于详情页）

**优点：**
- SurfaceView硬件加速，画质最佳
- 性能优秀
- 支持全屏播放

**缺点：**
- 快速滑动时可能有1-2帧延迟
- 多实例管理复杂

**适用场景：**
- 详情页视频播放
- 全屏播放
- 对画质要求高的场景

### Auto模式（推荐）

自动根据视频尺寸选择：
- 视频面积 < 屏幕40% → Canvas模式
- 视频面积 ≥ 屏幕40% → Layer模式

## 音频焦点策略

### 焦点类型说明

| 类型 | 说明 | 适用场景 |
|------|------|----------|
| GAIN | 长时间获取焦点 | 详情页播放、长视频 |
| TRANSIENT | 短暂获取焦点 | 列表预览、短视频 |
| TRANSIENT_MAY_DUCK | 可降低音量的短暂焦点 | 通知音、提示音 |
| EXCLUSIVE | 独占焦点（车机特有） | 需要独占音频的场景 |

### 车机场景建议

```javascript
// 列表流
const listPlayer = new HybridVideoPlayer({
    audioFocusType: 'transient',  // 短暂焦点
    enableDolby: false            // 不启用杜比
});

// 详情页
const detailPlayer = new HybridVideoPlayer({
    audioFocusType: 'gain',       // 长时间焦点
    enableDolby: true             // 启用杜比
});
```

## 杜比音效

### 支持的格式

- Dolby Digital (AC-3)
- Dolby Digital Plus (E-AC-3)
- Dolby Atmos (E-AC-3-JOC) - Android 9.0+
- Dolby AC-4 - Android 10+

### 启用杜比

```javascript
const player = new HybridVideoPlayer({
    enableDolby: true
});
```

### 注意事项

1. 需要车机硬件支持杜比解码
2. 完整杜比功能需要集成杜比SDK（需授权）
3. 当前实现使用Android标准AudioEffect增强

## 性能优化建议

### 1. 列表流优化

```javascript
// 降低帧率节省性能
const player = new HybridVideoPlayer({
    renderMode: 'canvas',
    maxFrameRate: 30,  // 降低到30fps
    enableDolby: false
});
```

### 2. 可见性管理

```javascript
// 使用IntersectionObserver监听可见性
const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (!entry.isIntersecting) {
            player.pause();  // 不可见时暂停
        }
    });
});
```

### 3. 资源释放

```javascript
// 页面卸载时销毁播放器
window.addEventListener('beforeunload', () => {
    player.destroy();
});
```

## 目录结构

```
app/src/main/
├── java/com/ace/webdemo/
│   ├── MainActivity.kt                    # 主Activity
│   └── player/
│       ├── HybridVideoPlayerManager.kt   # 播放器管理器
│       ├── config/                       # 配置类
│       │   ├── PlayerConfig.kt
│       │   ├── PlayerState.kt
│       │   ├── RenderMode.kt
│       │   └── AudioFocusType.kt
│       ├── renderer/                     # 渲染器
│       │   ├── VideoRenderer.kt          # 接口
│       │   ├── canvas/
│       │   │   └── CanvasVideoRenderer.kt
│       │   └── layer/
│       │       └── LayerVideoRenderer.kt
│       ├── audio/                        # 音频模块
│       │   ├── AudioFocusManager.kt
│       │   └── DolbyAudioProcessor.kt
│       └── bridge/                       # JSBridge
│           └── VideoPlayerJSBridge.kt
├── assets/
│   ├── js/
│   │   └── HybridVideoPlayer.js          # H5 SDK
│   └── demo.html                         # 演示页面
└── AndroidManifest.xml
```

## 运行演示

1. 编译项目：`./gradlew build`
2. 运行应用：`./gradlew installDebug`
3. 打开应用后会自动加载演示页面
4. 点击按钮测试各种功能

## 常见问题

### Q: Canvas模式下看不到视频？
A: 检查是否正确接收到帧数据回调，确认JSBridge通信正常。Canvas模式需要原生侧发送帧数据到H5。

### Q: Layer模式滚动有延迟？
A: 使用Choreographer已经优化到60fps，如果仍有延迟，可以尝试：
- 减少页面复杂度
- 启用硬件加速
- 使用Canvas模式

### Q: 如何支持完整的杜比音效？
A: 需要：
1. 集成杜比SDK（需要授权）
2. 车机硬件支持杜比输出
3. 配置ExoPlayer使用杜比音轨

### Q: 可以同时播放多个视频吗？
A: 可以，但需要注意：
- 音频焦点管理（同时只能有一个持有焦点）
- 性能影响（建议列表流使用Canvas模式+降帧）

## 后续优化方向

1. **Canvas模式优化**
   - 使用SharedMemory减少数据拷贝
   - 使用WebGL实现零拷贝纹理渲染
   - GPU加速YUV->RGB转换

2. **同层渲染优化**
   - 实现真正的WebView同层渲染
   - 优化位置同步算法

3. **杜比音效**
   - 集成官方杜比SDK
   - 支持更多音效参数配置

4. **性能监控**
   - 添加性能埋点
   - FPS监控
   - 内存监控

## 技术支持

如有问题，请查看：
- 架构文档：`docs/ARCHITECTURE.md`
- 示例代码：`app/src/main/assets/demo.html`
- 源码注释

## License

MIT License
